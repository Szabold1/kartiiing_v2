{
  "name": "kartiiing-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd shared-types && npm run build"
  },
  "workspaces": [
    "shared-types",
    "api",
    "kartiiing"
  ],
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "tailwindcss-animate": "^1.0.7"
  }
}
